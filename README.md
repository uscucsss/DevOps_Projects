# DevOps_Project1
eng The first DevOps project, which included: installing a VM (Ubuntu) server and setting up a bridged connection visible to the main computer; working in a terminal with basic Nginx, which serves a web page; SSH access, which allows you to manage Ubuntu in a familiar environment (PowerShell terminal).


ru Мой первый проект DevOps, в котором было реализовано: установка VM (Ubuntu) сервера и настройка мостового подключения, которое видно основному компьютеру; работа в терминале с базовым Nginx, который отдает страницу в сеть; SSH-доступ — через него можно управлять Ubuntu в привычном окружении (терминал PowerShell)
